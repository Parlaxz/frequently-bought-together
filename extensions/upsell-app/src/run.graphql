query RunInput {
  cart {
    lines {
      quantity
      merchandise {
        __typename
        ...on ProductVariant {
            id
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "upsellApp", key: "function-configuration"){
      value
    }
  }
}